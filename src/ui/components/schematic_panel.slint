import { Palette } from "std-widgets.slint";

// schematic rendering panel; schematic rendering lands in a later milestone,
// for now the panel is a neutral gray placeholder that owns the body space
export component SchematicPanel inherits Rectangle {
    background: Palette.alternate-background;
}
