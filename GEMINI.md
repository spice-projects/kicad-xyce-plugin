# Project Instructions

## Environment Management
- All Python operations (running tests, linting, executing the plugin) must be performed using the project's virtual environment located at `.venv`.
- Use `./.venv/bin/python` to execute scripts or run modules.
