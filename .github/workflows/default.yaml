name: C++ CI Workflow

on:
  pull_request:
    branches:
      - main
  push:

permissions:
  pull-requests: write
  contents: read

jobs:
  check-pr:
    if: github.event_name == 'push' && github.ref != 'refs/heads/main' && !startsWith(github.ref, 'refs/tags/')
    timeout-minutes: 5
    runs-on: ubuntu-latest
    outputs:
      has_pr: ${{ steps.check.outputs.has_pr }}
    steps:
      - name: Check for open PR
        id: check
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          response=$(curl -skf -H "Authorization: token $GITHUB_TOKEN" "${GITHUB_API_URL}/repos/${{ github.repository }}/pulls?head=${{ github.repository_owner }}:${{ github.ref_name }}&state=open&per_page=1" 2>&1)
          echo "API response: ${response}"
          count=$(echo "$response" | grep -c '"number"' || true)
          echo "has_pr=$( [ "$count" -gt 0 ] && echo true || echo false )" >> $GITHUB_OUTPUT

  build-and-test:
    name: Build & Test (${{ matrix.os }})
    needs: [check-pr]
    if: |-
      !cancelled() &&
      (github.event_name != 'push' || github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') || needs.check-pr.outputs.has_pr != 'true')
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: macos-26
            platform: macos-arm64
            build_enabled: true
          - os: macos-26-intel
            platform: macos-x64
            build_enabled: true
          - os: windows-latest
            platform: windows-x64
            build_enabled: true
          - os: ubuntu-latest
            platform: ubuntu-x64
            build_enabled: false
    runs-on: ${{ matrix.os }}
    env:
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      VCPKG_BINARY_SOURCES: 'clear;files,${{ github.workspace }}/.vcpkg-cache,readwrite'
    steps:
      - name: Checkout repository
        if: ${{ matrix.build_enabled }}
        uses: actions/checkout@v7

      - name: Run clang-format check
        if: ${{ matrix.build_enabled && matrix.os == 'macos-26' }}
        shell: bash
        env:
          HOMEBREW_NO_REQUIRE_TAP_TRUST: 1
        run: |
          if ! command -v clang-format >/dev/null 2>&1; then
            brew install clang-format
          fi
          bash build/check-format.sh

      - name: Setup Ninja
        if: ${{ matrix.build_enabled }}
        shell: bash
        run: pip install ninja

      - name: Restore vcpkg binary cache
        if: ${{ matrix.build_enabled }}
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/.vcpkg-cache
          key: vcpkg-${{ matrix.os }}-build-${{ hashFiles('vcpkg.json') }}-${{ hashFiles('vcpkg-configuration.json') }}
          restore-keys: |
            vcpkg-${{ matrix.os }}-build-

      - name: Setup vcpkg
        if: ${{ matrix.build_enabled }}
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgJsonGlob: 'vcpkg.json'

      - name: Restore CMake build cache
        if: ${{ matrix.build_enabled }}
        uses: actions/cache@v5
        with:
          path: |
            ${{ github.workspace }}/.build-release
            ${{ github.workspace }}/.ccache
          key: cmake-${{ matrix.os }}-build-${{ hashFiles('vcpkg-configuration.json') }}-${{ hashFiles('vcpkg.json') }}-${{ hashFiles('tests/CMakeLists.txt') }}-${{ hashFiles('CMakeLists.txt') }}-${{ hashFiles('CMakePresets.json') }}
          restore-keys: |
            cmake-${{ matrix.os }}-build-

      - name: Setup ccache
        if: ${{ matrix.build_enabled }}
        shell: bash
        run: |
          if ! command -v ccache >/dev/null 2>&1; then
            if [ "$RUNNER_OS" = "macOS" ]; then
              brew install ccache
            elif [ "$RUNNER_OS" = "Linux" ]; then
              sudo apt-get install -y -qq ccache
            elif [ "$RUNNER_OS" = "Windows" ]; then
              choco install ccache
            fi
          fi

      - name: Setup MSVC dev environment
        if: ${{ matrix.build_enabled && runner.os == 'Windows' }}
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
          validate: false

      - name: Configure CMake
        if: ${{ matrix.build_enabled && hashFiles('.build-release/CMakeCache.txt') == '' }}
        shell: bash
        run: cmake --preset release

      - name: Build C++ Target
        if: ${{ matrix.build_enabled }}
        run: cmake --build --preset release

      - name: Run C++ Tests
        if: ${{ matrix.build_enabled }}
        shell: bash
        run: |
          if [ "$RUNNER_OS" = "Windows" ]; then
            ./.build-release/tests/kicad-xyce-plugin-tests.exe --gtest_filter=-Ui*
          else
            ./.build-release/tests/kicad-xyce-plugin-tests
          fi

      - name: Gather runtime dependencies
        if: ${{ matrix.build_enabled }}
        run: cmake --install .build-release --component RuntimeDeps --prefix .build-release

      - name: Upload build artifact
        if: ${{ matrix.build_enabled }}
        uses: actions/upload-artifact@v7
        with:
          name: kicad-xyce-plugin-${{ matrix.platform }}
          path: .build-release/artifacts/
          if-no-files-found: error
          retention-days: 7

  coverage:
    name: C++ Coverage
    needs: [check-pr]
    if: |-
      !cancelled() &&
      github.repository == 'spice-projects/kicad-xyce-plugin' &&
      (github.event_name != 'push' || needs.check-pr.outputs.has_pr == 'true') &&
      (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    permissions:
      contents: read
      pull-requests: read
      code-quality: write
    uses: ./.github/workflows/coverage.yml

  macos-app-bundle:
    name: macOS App Bundle
    needs: [build-and-test]
    if: |-
      !cancelled() &&
      (github.event_name != 'push' || github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') || needs.check-pr.outputs.has_pr != 'true')
    runs-on: macos-26
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Download arm64 artifact
        uses: actions/download-artifact@v8
        with:
          name: kicad-xyce-plugin-macos-arm64
          path: artifacts/arm64

      - name: Download x64 artifact
        uses: actions/download-artifact@v8
        with:
          name: kicad-xyce-plugin-macos-x64
          path: artifacts/x64

      - name: Create macOS app bundle
        run: |
          APP_BUNDLE="artifacts/KiCad Xyce Plugin.app"
          rm -rf "$APP_BUNDLE"
          mkdir -p "$APP_BUNDLE/Contents/MacOS" "$APP_BUNDLE/Contents/Resources"

          cp "plugin-icon-64x64.png" "$APP_BUNDLE/Contents/Resources/plugin-icon-64x64.png"

          lipo -create \
            -output "$APP_BUNDLE/Contents/MacOS/kicad-xyce-plugin" \
            "artifacts/arm64/kicad-xyce-plugin" \
            "artifacts/x64/kicad-xyce-plugin"

          for dylib in artifacts/arm64/*.dylib artifacts/x64/*.dylib; do
            [ -f "$dylib" ] || continue
            cp "$dylib" "$APP_BUNDLE/Contents/MacOS/"
          done

          cat > "$APP_BUNDLE/Contents/Info.plist" <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key>
              <string>kicad-xyce-plugin</string>
              <key>CFBundleIdentifier</key>
              <string>com.spice-projects.kicad-xyce-plugin</string>
              <key>CFBundleIconFile</key>
              <string>plugin-icon-64x64.png</string>
              <key>CFBundleName</key>
              <string>KiCad Xyce Plugin</string>
              <key>CFBundleDisplayName</key>
              <string>KiCad Xyce Plugin</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>CFBundleVersion</key>
              <string>1</string>
          </dict>
          </plist>
          EOF

          chmod +x "$APP_BUNDLE/Contents/MacOS/kicad-xyce-plugin"
          install_name_tool -add_rpath @executable_path "$APP_BUNDLE/Contents/MacOS/kicad-xyce-plugin" 2>/dev/null || true
          codesign --force --sign - "$APP_BUNDLE/Contents/MacOS/kicad-xyce-plugin" 2>/dev/null || true

      - name: Upload macOS app bundle artifact
        uses: actions/upload-artifact@v7
        with:
          name: KiCad Xyce Plugin.app
          path: artifacts/KiCad Xyce Plugin.app
          if-no-files-found: error
          retention-days: 7

  package-and-artifacts:
    name: Package & Artifacts
    needs: [build-and-test, macos-app-bundle]
    if: |-
      !cancelled() &&
      (github.event_name != 'push' || github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') || needs.check-pr.outputs.has_pr != 'true')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Extract version from tag
        id: version
        run: echo "version=$(echo '${{ github.ref_name }}' | sed 's/^v//')" >> $GITHUB_OUTPUT

      - name: Download Windows artifact
        uses: actions/download-artifact@v8
        with:
          name: kicad-xyce-plugin-windows-x64
          path: artifacts/windows

      - name: Download macOS app bundle artifact
        uses: actions/download-artifact@v8
        with:
          name: KiCad Xyce Plugin.app
          path: artifacts/macos

      - name: Package Windows
        run: |
          dlls=$(ls artifacts/windows/*.dll 2>/dev/null | tr '\n' ' ')
          bash build/create-kicad-package.sh '${{ steps.version.outputs.version }}' artifacts/windows/kicad-xyce-plugin.exe kicad-xyce-plugin.exe windows "$dlls"
          mkdir -p packages
          mv dist/kicad-xyce-plugin-${{ steps.version.outputs.version }}.zip \
             packages/kicad-xyce-plugin-${{ steps.version.outputs.version }}-windows-x64.zip

      - name: Package macOS app bundle
        run: |
          APP_BUNDLE="artifacts/macos/KiCad Xyce Plugin.app"
          mkdir -p packages
          cd artifacts/macos && zip -r -y "$GITHUB_WORKSPACE/packages/kicad-xyce-plugin-${{ steps.version.outputs.version }}-macos-app-bundle.zip" \
            "KiCad Xyce Plugin.app"

      - name: Upload Windows package artifact
        uses: actions/upload-artifact@v7
        with:
          name: kicad-xyce-plugin-${{ steps.version.outputs.version }}-windows-x64-package
          path: packages/kicad-xyce-plugin-${{ steps.version.outputs.version }}-windows-x64.zip
          if-no-files-found: error
          retention-days: 7

      - name: Upload macOS app bundle package artifact
        uses: actions/upload-artifact@v7
        with:
          name: kicad-xyce-plugin-${{ steps.version.outputs.version }}-macos-app-bundle-package
          path: packages/kicad-xyce-plugin-${{ steps.version.outputs.version }}-macos-app-bundle.zip
          if-no-files-found: error
          retention-days: 7

  upload-to-release:
    name: Upload Packages to GitHub Release
    needs: [package-and-artifacts]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Extract version from tag
        id: version
        run: echo "version=$(echo '${{ github.ref_name }}' | sed 's/^v//')" >> $GITHUB_OUTPUT

      - name: Download Windows package artifact
        uses: actions/download-artifact@v8
        with:
          name: kicad-xyce-plugin-${{ steps.version.outputs.version }}-windows-x64-package
          path: packages/windows

      - name: Download macOS app bundle package artifact
        uses: actions/download-artifact@v8
        with:
          name: kicad-xyce-plugin-${{ steps.version.outputs.version }}-macos-app-bundle-package
          path: packages/macos

      - name: Upload packages to GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload '${{ github.ref_name }}' --clobber \
            packages/windows/kicad-xyce-plugin-${{ steps.version.outputs.version }}-windows-x64.zip \
            packages/macos/kicad-xyce-plugin-${{ steps.version.outputs.version }}-macos-app-bundle.zip
